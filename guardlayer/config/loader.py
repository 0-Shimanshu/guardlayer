"""Configuration loader and reloader.

This module should read YAML config, merge with defaults, expose typed access,
and support change detection for runtime rule/threshold updates.
"""

"""Configuration loader and reloader."""
