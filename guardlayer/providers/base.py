"""Base AI provider interface.

This module should define a provider contract returning a bounded risk delta
for borderline cases: evaluate(event, context, current_score) -> float.
"""

"""Base interface for AI providers."""
