"""Base contract for all signal extractors.

This module should define the abstract extractor interface with required
metadata (name, category, weight) and an extract(event, context) operation.
"""

"""Base interfaces for signal extractors."""
